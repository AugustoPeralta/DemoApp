{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "new-version": "lerna version --conventional-commits --yes",
    "test": "lerna run test ",
    "test-common": "lerna run test --scope=nodedemoapp",
    "ci:build": "lerna run ci:build",
    "ci:zip": "lerna run pack",
    "changed": "lerna changed "
  }
}
